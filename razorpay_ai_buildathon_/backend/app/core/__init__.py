"""
Core package — reserved for Phase 2.

Will contain:
  - deterministic reconciliation engine
  - matching cascade (exact → composite → fuzzy → agent)
  - decision engine
  - confidence scoring
"""
